<p class="text-xs font-medium text-gray-500 mb-0">Tags: <span class="font-bold">
  {% for tag in postOne-tags %}
    {{ tag }}{% unless forloop.last %}, {% endunless %}
  {% endfor %}
</span></p>
<p class="text-xs font-medium text-gray-500 mb-0">{{postOne.data.date | readableDate}} | {{ postOne.templateContent | timeToRead }} read</p>