---
title: "JSON API"
permalink: '/api.json'
---
{
  "posts": [
    {% for post in collections.hash-post %}
      {
        "title": "{{ post.data.title }}",
        "url": "https://dai-global-developments.com/post-api/{{ post.data.title | slugify }}.json",
      }
    {% endfor %}
  ]
}